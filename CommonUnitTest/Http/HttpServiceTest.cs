namespace CommonUnitTest.Http
{
    public class HttpServiceTest
    {
        // TODO

        // ~ T Get<T>(string endPoint)
        // Refactor to testable design
        // : (HttpWebRequest)WebRequest.Create(endPoint);
        // : new StreamReader(dataStream))
    }
}
